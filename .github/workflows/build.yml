name: Build ImmortalWrt

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 开始编译
        uses: P3TERX/Actions-OpenWrt@main
        with:
          config-file: .config
